// Write a function that takes a string
// It should count all characters
// It should output the most frequent, with it's frequency
// Add another optional parameter that allows the output to include n numbers of characters with its corresponding frequency, in descending order